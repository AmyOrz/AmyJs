export declare class Main {
    private static _parentId;
    private static _config;
    private static _canvasId;
    static setCanvas(canvasId?: string, parentId?: string): typeof Main;
    static init(): typeof Main;
}
