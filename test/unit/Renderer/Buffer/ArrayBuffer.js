describe("Buffer", function () {
    var sandbox = null;
    var arrayBuffer = null;

    beforeEach(function () {
        sandbox = sinon.sandbox.create();
        arrayBuffer = Amy.
    });
    afterEach(function () {

    });

    describe("method", function () {

    });
});
